{% extends 'base.html.twig' %}

{% block title %}toutes les recettes{% endblock %}

{% block body %}
    <p>
        php -S localhost:8000 -t public 
    </p>
    <ul>
    // recupere current router
        {{dump(app.current_route)}}
    // recupere request 
        {{dump(app.request)}}
        <li>
        <a href="{{ path('recipe.show', {id: 32, slug: 'pate-saumon'}) }}">Pates au saumon</a>
        </li>
    </ul>
{% endblock %}

